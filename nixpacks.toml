[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd PatientPro && python manage.py collectstatic --noinput && gunicorn PatientPro.wsgi --bind 0.0.0.0:$PORT"
