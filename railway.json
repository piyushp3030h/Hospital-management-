{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "providers": ["python"]
    },
    "watchPatterns": ["PatientPro/**"]
  },
  "deploy": {
    "startCommand": "cd PatientPro && python manage.py collectstatic --noinput && gunicorn PatientPro.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
